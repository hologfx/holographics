// NAME="Gradient"
// DESCRIPTION="This is an example of a really simple theme with gradients. Make sure to set both background colors for maximum effect!"

body, html{
	font-family: 'Lato';
	font-style: normal;
	font-weight: 300;
	color: $text_a;
}

h2 {
	font-family: 'Karla';
	font-style: normal;
	font-weight: 700;
}

i {
	margin-right: 1vw;
}

.widget {
  background-image: linear-gradient(to right, darken($primary_color, 10%), lighten($secondary_color, 10%));
	background-position: center center;

  border: 1px solid $background_a;
}